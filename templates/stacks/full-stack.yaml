---
# Full Stack Template
# Production-ready infrastructure: VPC, tiered security groups, EKS with
# add-ons, PostgreSQL on RDS, application secrets and monitoring.
#
# Usage: copy into an account/region directory as a top-level stack and set
# settings.context.environment (the stack name is <tenant>-<stage>-<environment>):
#   cp templates/stacks/full-stack.yaml stacks/orgs/fnx/prod/eu-west-2/platform-01.yaml
#   atmos validate stacks
#   atmos workflow plan -f plan-environment -s fnx-prod-platform-01
# To target another account or region, change the `orgs/fnx/<stage>/_defaults`
# (org and account defaults) and `mixins/region/<region>` imports.
# Environment-wide knobs live in settings.environment; components read them
# as {{ .settings.environment.<key> }}. Cross-component values use
# !terraform.state and deployment order comes from dependencies.components.

import:
  - orgs/fnx/prod/_defaults
  - mixins/region/eu-west-2
  - mixins/production
  - catalog/_base/defaults
  - catalog/kms/defaults
  - catalog/vpc/defaults
  - catalog/securitygroup/defaults
  - catalog/eks/defaults
  - catalog/eks-addons/defaults
  - catalog/secretsmanager/defaults
  - catalog/monitoring/defaults
  # Optional environment profile from templates/configs (import it last):
  # - mixins/profiles/production

vars:
  tags:
    Template: "full-stack"

settings:
  context:
    environment: platform-01
  environment:
    description: "Full production platform (EKS, RDS, monitoring)"
    vpc_cidr: "10.30.0.0/16"
    eks_kubernetes_version: "1.36"
    eks_public_access: false
    rds_engine: "postgres"
    rds_engine_version: "16"
    rds_family: "postgres16"
    rds_instance_class: "db.r6g.large"
    log_retention_days: 30
    ops_email: "ops@example.com"
    # Route 53 zone external-dns and cert-manager may write to
    hosted_zone_id: "Z0123456789ABCDEFGHIJ"

components:
  terraform:
    # Core networking: one NAT gateway per AZ for high availability
    vpc/main:
      metadata:
        component: vpc
        inherits:
          - vpc/defaults
      vars:
        vpc_cidr: "{{ .settings.environment.vpc_cidr }}"
        public_subnets:
          - "10.30.1.0/24"
          - "10.30.2.0/24"
          - "10.30.3.0/24"
        private_subnets:
          - "10.30.10.0/24"
          - "10.30.20.0/24"
          - "10.30.30.0/24"
        database_subnets:
          - "10.30.100.0/24"
          - "10.30.101.0/24"
          - "10.30.102.0/24"
        enable_nat_gateway: true
        nat_gateway_strategy: "one_per_az"
        enable_flow_logs: true
        flow_logs_retention_days: 90

    # Application Load Balancer tier
    securitygroup/alb:
      metadata:
        component: securitygroup
        inherits:
          - securitygroup
      vars:
        security_groups:
          alb:
            description: "Application Load Balancer"
            ingress_rules:
              - description: "HTTP from internet (redirected to HTTPS)"
                from_port: 80
                to_port: 80
                protocol: "tcp"
                cidr_blocks: ["0.0.0.0/0"]
              - description: "HTTPS from internet"
                from_port: 443
                to_port: 443
                protocol: "tcp"
                cidr_blocks: ["0.0.0.0/0"]

    # Additional security group for EKS worker nodes (the cluster security
    # group itself is created by EKS)
    securitygroup/eks-nodes:
      metadata:
        component: securitygroup
        inherits:
          - securitygroup
      vars:
        security_groups:
          eks-nodes:
            description: "EKS node group security group"
            ingress_rules:
              - description: "Node to node communication"
                from_port: 0
                to_port: 65535
                protocol: "tcp"
                self: true
              - description: "Traffic from the load balancers"
                from_port: 1025
                to_port: 65535
                protocol: "tcp"
                security_groups:
                  - !terraform.state securitygroup/alb .security_group_ids.alb
      dependencies:
        components:
          - component: vpc/main
          - component: securitygroup/alb

    # Database tier: PostgreSQL only from the EKS nodes
    securitygroup/rds:
      metadata:
        component: securitygroup
        inherits:
          - securitygroup
      vars:
        security_groups:
          rds:
            description: "RDS security group"
            ingress_rules:
              - description: "PostgreSQL from EKS nodes"
                from_port: 5432
                to_port: 5432
                protocol: "tcp"
                security_groups:
                  - !terraform.state securitygroup/eks-nodes .security_group_ids.eks-nodes
      dependencies:
        components:
          - component: vpc/main
          - component: securitygroup/eks-nodes

    # EKS cluster (private endpoint; subnet_ids come from eks/defaults)
    eks/main:
      metadata:
        component: eks
        inherits:
          - eks/defaults
      vars:
        name: main
        cluster_kubernetes_version: "{{ .settings.environment.eks_kubernetes_version }}"
        cluster_endpoint_private_access: true
        cluster_endpoint_public_access: "{{ .settings.environment.eks_public_access }}"
        associated_security_group_ids:
          - !terraform.state securitygroup/eks-nodes .security_group_ids.eks-nodes
        node_groups:
          general:
            instance_types: ["m6i.large", "m6i.xlarge"]
            capacity_type: "ON_DEMAND"
            min_group_size: 2
            max_group_size: 10
            desired_group_size: 3
      dependencies:
        components:
          - component: vpc/main
          - component: securitygroup/eks-nodes

    # EKS add-ons, wired to the cluster outputs
    eks-addons/main:
      metadata:
        component: eks-addons
        inherits:
          - eks-addons
      vars:
        cluster_name: !terraform.state eks/main .eks_cluster_id
        host: !terraform.state eks/main .eks_cluster_endpoint
        cluster_ca_certificate: !terraform.state eks/main .eks_cluster_certificate_authority_data
        oidc_provider_arn: !terraform.state eks/main .eks_cluster_identity_oidc_issuer_arn
        oidc_provider_url: !terraform.state eks/main .eks_cluster_identity_oidc_issuer
        clusters:
          main:
            enable_aws_load_balancer_controller: true
            vpc_id: !terraform.state vpc/main .vpc_id
            enable_cluster_autoscaler: true
            enable_external_dns: true
            dns_zone_ids:
              - "{{ .settings.environment.hosted_zone_id }}"
            enable_cert_manager: true
            cert_manager_letsencrypt_email: "{{ .settings.environment.ops_email }}"
            enable_metrics_server: true
            # CloudWatch agent + Fluent Bit; log groups on kms/main
            enable_container_insights: true
            container_insights_kms_key_arn: !terraform.state kms/main .key_arn
            container_insights_log_retention_days: "{{ .settings.environment.log_retention_days }}"
      dependencies:
        components:
          - component: eks/main
          - component: vpc/main
          - component: kms/main

    # PostgreSQL on RDS. The master password is managed by RDS in Secrets
    # Manager (output: password_secret_arn); no password lives in config.
    rds/main:
      metadata:
        component: rds
      vars:
        identifier: "{{ .settings.context.tenant }}-{{ .settings.environment.account }}-{{ .settings.context.environment }}-db"
        engine: "{{ .settings.environment.rds_engine }}"
        engine_version: "{{ .settings.environment.rds_engine_version }}"
        family: "{{ .settings.environment.rds_family }}"
        instance_class: "{{ .settings.environment.rds_instance_class }}"
        username: "app_admin"
        port: 5432
        db_name: "app"
        vpc_id: !terraform.state vpc/main .vpc_id
        vpc_cidr: !terraform.state vpc/main .vpc_cidr
        subnet_ids: !terraform.state vpc/main .private_subnet_ids
        allowed_security_groups:
          - !terraform.state securitygroup/rds .security_group_ids.rds
        allocated_storage: 100
        max_allocated_storage: 1000
        storage_encrypted: true
        multi_az: true
        deletion_protection: true
        skip_final_snapshot: false
        backup_retention_period: 7
        backup_window: "03:00-04:00"
        maintenance_window: "sun:04:00-sun:05:00"
        performance_insights_enabled: true
      dependencies:
        components:
          - component: vpc/main
          - component: securitygroup/rds

    # The stack's customer-managed key (Container Insights log groups)
    kms/main:
      metadata:
        component: kms
        inherits:
          - kms/defaults
      vars:
        description: Platform main encryption key (CloudWatch Logs)
        alias_name: "{{ .settings.context.environment }}-main"

    # Application secrets (random values generated by Terraform)
    secretsmanager/app:
      metadata:
        component: secretsmanager
        inherits:
          - secretsmanager/api

    # Dashboards and alarms for the platform
    monitoring/main:
      metadata:
        component: monitoring
        inherits:
          - monitoring/defaults
      vars:
        create_dashboard: true
        eks_cluster_name: !terraform.state eks/main .eks_cluster_id
        enable_rds_monitoring: true
        rds_instances:
          - !terraform.state rds/main .instance_id
        alarm_email_subscriptions:
          - "{{ .settings.environment.ops_email }}"
      dependencies:
        components:
          - component: vpc/main
          - component: eks/main
          - component: rds/main
